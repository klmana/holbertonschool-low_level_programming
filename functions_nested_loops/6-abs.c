#include "main.h"
#include <stdlib.h>
/**
*_abs - computes the absolute value of an integer
*@x: of the appropriate integer type for the function
*Return: Always 0
*/
int _abs(int x)
{
	int i = abs(x);

	return (i);
}
