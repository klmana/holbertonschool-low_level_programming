#include <stdio.h>

/**
*main - prints the lowercase alphabet in reverse, followed by a new line.
*Return: always 0 when success
*/
int main(void)
{
char alphabet;
for (alphabet = 'z'; alphabet >= 'a'; alphabed--)
{
putchar (alphabet);
}
putchar ('\n')
return (0);
}
